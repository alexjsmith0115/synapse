[pytest]
timeout = 10
timeout_method = thread
testpaths = tests
pythonpath = src tests/unit tests
markers =
    integration: marks tests as integration tests (require FalkorDB + C# LSP)
    api_integration: marks API HTTP integration tests (require Memgraph)
    e2e: marks browser-level E2E tests (require live server + Chromium)
